# ═══════════════════════════════════════════════════════════
# Central Port Configuration - Single Source of Truth
# ═══════════════════════════════════════════════════════════
# All services reference these ports to avoid conflicts
# Edit these values to change ports across the entire application

# Frontend (Vite dev server)
FRONTEND_PORT=3000

# Backend (Express API)  
BACKEND_PORT=5000

# WebSocket
WS_PORT=5000

# IMPORTANT: If port 5000 is occupied by macOS AirPlay Receiver:
# Go to System Settings → General → AirDrop & Handoff
# Turn OFF "AirPlay Receiver"
